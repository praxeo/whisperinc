<Window x:Class="WhisperInk.ContextBiasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Context Bias Terms" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" ResizeMode="CanResizeWithGrip">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="CaretBrush" Value="#CCCCCC"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="#CCC"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="4,0"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Context Bias Terms (one per line):" 
                   FontWeight="Bold" FontSize="12" Foreground="#AAAAAA" Margin="0,0,0,10"/>
        
        <TextBox Grid.Row="1" x:Name="TxtBiasTerms"
                 AcceptsReturn="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto"
                 Text="Enter domain-specific terms here, one per line.&#x0a;These terms help the transcription model recognize specialized vocabulary.&#x0a;&#x0a;Example for medical:&#x0a;myocardial infarction&#x0a;electrocardiogram&#x0a;tachycardia&#x0a;bradycardia"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" Click="BtnCancel_Click"/>
            <Button Content="Save" Click="BtnSave_Click" 
                    Background="#2A4A2A" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
